Ícones de 16px das ações de documento: arquivo, exportação, navegação de registros, dados e sistema.

- Uso: `<i class="rp-ico rp-ico-NOME"></i>` (o CSS já traz o desenho); os SVGs também estão no grupo de ativos Ferramentas.
- Estilo: contorno `nav-divider` de 1,1px, preenchimentos claros (`field`, `field-readonly`, `button`) e um acento `gold`, verde para "adicionar/exportar", vermelho só para PDF e excluir.
- Tamanho sempre `icon` (16px); em destaque, 32px — nunca tamanhos intermediários.
